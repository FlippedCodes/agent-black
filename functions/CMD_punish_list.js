const errHander = (err) => { console.error('ERROR:', err); };

module.exports.run = async (client, message, args, config) => {

};

module.exports.help = {
  name: 'CMD_list_punish',
  parent: 'punish',
};
