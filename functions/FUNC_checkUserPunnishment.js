module.exports.run = async () => {
  // lookup if threshhold has been passed
  // check if passed
  // check if ongoing punnishment is already in process with same punnishmentID
  // return result
};

module.exports.help = {
  name: 'FUNC_checkUserPunnishment',
};
