module.exports.run = async (client, message, args, config) => {
  // check permissions
  // check server settings (is punnishing feature enabled?)
  // command handler for subcommands: manualAdd, manualRemove, stats, userStats (calls lookup), add, remove, list, listPunnishments
};

module.exports.help = {
  name: 'punnish',
  desc: 'Punnishes a user.',
};
