// is used to configure settings
// if setting is not set, use default from config
module.exports.run = async (client, message, args, config) => {
  // check permissions (Admin)
  // command handler for subcommands: list (list settings), enable, pointLifetime, listReasons, addReason, removeReason, listPunnishment, addPunnishment, removePunnishment
};

module.exports.help = {
  name: 'punnishsettings',
  desc: 'Managing command for setting up pointslists.',
};
